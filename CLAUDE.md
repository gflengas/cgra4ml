# Working Agreement

- Work has moved through several branches (`brevitas-qonnx-backend` → `brevitas-golden-ref` → `brevitas-rtl-e2e`), so there's no single fixed branch name to enforce here anymore — work on whichever branch the active task/plan names (check `docs/superpowers/plans/` or `.superpowers/sdd/` for the current one).
- Do not `git commit` or `git push` anything — the user handles commits and pushes themselves.
  - (During the 2026-08-11 RTL end-to-end work the user granted a one-off exception for that plan's branch, `brevitas-rtl-e2e`, allowing subagents to commit locally but never push. That was per-plan permission given in-session, not a standing change to this rule — ask again next time.)

# Known Issues

*(These remain deprioritized now that the pipeline is genuinely end-to-end — see the 2026-08-11 Progress Log entry. RTL simulation passes for the XOR model via the Docker-contained Verilator toolchain; none of the issues below block that. Revisit if/when the next model needs conv/pooling/residual/non-piecewise-linear activations, which the current adapter and JSON schema don't cover.)*

## `quantized_model.export_graph_json()` (deepsocflow/py/brevitas/ptq.py) is not sufficient for fixed-point simulation yet

The JSON it produces (e.g. `deepsocflow/py/brevitas/model/xor_graph.json`) has enough data for the matmul/bias-add/rescale part of a fixed-point simulator, but not for the activation functions actually used, and the schema has several gaps that don't show up on the XOR model but will on anything bigger.

- **No way to execute non-piecewise-linear activations (`SiLU`, `Tanh`, `Sigmoid`, `GELU`, `SELU`) in pure integer arithmetic.** The JSON records `"activation": "silu"` plus its `bits`/`frac`/`scale`/`zero_point`, but nothing that tells a simulator how to map a quantized *input* to a quantized *output* through a curved function. `shift_round` (the round-to-nearest-even right-shift used everywhere else) only works for piecewise-linear functions through the origin like ReLU/LeakyReLU (see `quant_lrelu`, `deepsocflow/c/runtime.h:153`) - it can't compute `x * sigmoid(x)` or `tanh(x)`. The legacy qkeras backend's `_build_activation`/`XActivation` (`deepsocflow/py/xlayers.py:14-59`) confirms this was never solved even before this backend existed: it only implements `None`/`'relu'` (with power-of-two `negative_slope`) and raises `ValueError` for anything else - and `quant_lrelu` in the C firmware has no other activation path at all, so **this isn't just a JSON-schema gap, it's a real capability gap that also exists at the C/RTL deployment layer**, not something a Python-only fix can close by itself.
  - **Scoping decision (2026-08-10):** this backend's job is to port qkeras → QONNX at feature parity, not add new capability - so until the C runtime supports something better than `quant_lrelu`, only `ReLU`/`LeakyReLU` (power-of-two `negative_slope`) and `Identity` are "real" (deployable) activations. `SiLU`/`Tanh`/etc. stay supported in the *brevitas* model (fake-quantization only, via `ACT_MAP` in `ptq.py`) for QONNX export, but should not be used in models intended for eventual hardware deployment until this is resolved. `xor.py`'s `SiLU` should be swapped for `nn.LeakyReLU` (slope restricted to a negative power of two, e.g. `0.125`) to make the XOR example actually representative of what's deployable; `ptq.py` should assert the power-of-two constraint (mirroring `xlayers.py:23`) so a non-power-of-two slope fails loudly at quantization time instead of producing a model that can't be deployed.
  - **Researched design for when this gets picked up** (measured empirically against the real `xor_graph.json` fixed-point pipeline and against hls4ml's activation LUT implementations, not just theorized):
    - A pure value **LUT** (index = quantized input level → output level) is exact given its input quantization, and needs an `input_quant` added to the brevitas activation (`QuantSiLU(..., input_quant=Int8ActPerTensorFixedPoint)`) so calibration picks the table's domain/precision from real data - this input quantizer doesn't exist in the model today. hls4ml's newer `UnaryLUT`/HGQ2 path (`hls4ml/converters/keras_v3/hgq2/unary_lut.py`) does exactly this, plus a hardware trick worth reusing: it addresses the table directly by the *raw two's-complement bits* of the quantized input (table pre-permuted at export time: positive half first, negative half second) rather than by `input + 2^(bits-1)`, so no adder is needed for indexing.
    - Plain **PWL** (multiple linear segments, evaluated on the raw accumulator, no new quantization point needed) never reaches bit-exactness no matter how many segments are added (~1 LSB mismatch on 10-25% of values even at 13-21 segments) - it trades a *different*, irreducible error for not needing `input_quant`.
    - Best option found: a **"coefficient LUT"** - a small table of `(slope, intercept)` pairs indexed by the top bits of the input (via a free right-shift, no comparator chain, no search), with `slope` restricted to a sum of ≤2 signed powers of two (shift+add only, no real multiplier - keeps the same "shift-only" hardware philosophy as `quant_lrelu`/every other rescale in this project). Measured: 64 uniform segments (6-bit index) gets SiLU to 2.63% mismatch / tanh to 3.72% mismatch against the exact function, in ~128 bytes of table - beats a pure 1024-byte (10-bit) value-LUT's 4.16% mismatch on both storage and accuracy. `tanh`/`sigmoid` being odd/point-symmetric about their center lets the table (LUT or coefficient-LUT alike) be halved again by mirroring around `x=0`, `SiLU` does not have that symmetry.
    - A "range-split" idea (fine LUT only over the curved center, trivial constant/slope-1 formula for the saturated/asymptotic tails) was tried and **measured to perform worse**, not better: neither SiLU nor tanh flattens out until fairly far from the origin (`SiLU(-6) ≈ -0.015`, `tanh(2.5) ≈ 0.987`), so the "interesting" region already spans nearly the full useful input range - there's no narrow curved island to isolate, and the naive split ends up needing about as much table budget as just doing the whole range properly.
- ~~**No `signed`/`unsigned` flag per tensor.**~~ **Closed 2026-08-10** - `export_graph_json` now emits `input_signed`/`weight.signed`/`bias.signed`/`act_signed` per tensor (`ptq.py`), sourced from brevitas's own `QuantTensor.signed`/`act_quant.is_signed`. `sim.py` reads `act_signed` directly, falling back to the old name-based `UNSIGNED_ACTIVATIONS` set only for JSONs exported before this change.
- **No skip/residual-connection support in the JSON schema.** `"input"` is a single layer name or `null`; there's no `"skip_from"` the way the JSON-driven `Model._build_from_json` (`deepsocflow/py/brevitas/utils.py`) has for `residual_add`.
- **No conv/pooling hyperparameters in the export.** `export_graph_json` only ever emits `"type": "linear"` (or `"conv"` as a placeholder) - kernel_size/stride/padding for conv layers, and pooling layers entirely (`_quantize_pool`/`POOL_MAP` in `ptq.py` exist but aren't wired into `quantized_model`'s build loop or the JSON export), are not covered.
- **Rounding mode isn't recorded explicitly.** It's implicitly round-to-nearest-even (brevitas's default, confirmed via the exported QONNX `Quant` node's `rounding_mode = ROUND` attribute), but the JSON itself doesn't say so.

## ~~`ptq.py` doesn't validate that `LeakyReLU.negative_slope` is a power of two~~ Closed 2026-08-11

`quant_lrelu` (`deepsocflow/c/runtime.h:153`) computes the negative-side scaling as a pure left-shift (`x << pl_scale`), so it can only implement slopes that are exact negative powers of two (`0.5`, `0.25`, `0.125`, ...) - `xlayers.py:23` enforces this in the legacy backend with an explicit assert. `ptq.py`'s `_quantize_activation`/`ACT_MAP` used to map `nn.LeakyReLU` straight to `QuantLeakyReLU` with no such check, so it was possible to build and quantize a model with e.g. `nn.LeakyReLU(0.1)` that trains fine and calibrates fine, but silently produces a model that has no valid `quant_lrelu` parameterization and can't actually be deployed - the mismatch would only surface much later (whenever someone tries to derive `pl_scale = -log2(slope)` for the C/RTL side and gets a non-integer). **Fixed**: `_quantize_activation` (`ptq.py`) now asserts `negative_slope` is a negative power of two before building `QuantLeakyReLU`, mirroring `xlayers.py:23` and `adapter.py::act_params`'s existing (later-stage) check - a non-power-of-two slope, including the `nn.LeakyReLU()` default of `0.01`, now fails loudly at quantization time with an assertion message naming the bad value, instead of silently producing an undeployable model.

## ~~`xor.py`'s training is not reproducible despite `torch.manual_seed`~~ Closed 2026-08-11

`train(model, ..., seed=0, ...)` (`deepsocflow/py/brevitas/xor.py`) used to call `torch.manual_seed(seed)` as its first line, but by then `XOR()`'s `nn.Linear` layers had already drawn their random initial weights from the global RNG - `model = XOR()` ran *before* `train(model)` was called in `__main__`. So `manual_seed` only affected whatever nondeterminism existed inside `train()` itself (none, here - full-batch Adam is deterministic given fixed initial weights), not the actual source of run-to-run variance. Since XOR's loss landscape has dead-ReLU local optima, different unseeded initial weights sometimes converged to the correct function (`[0,1,1,0]`) and sometimes to a degenerate one (e.g. `[0,0,0,0]`) - discovered as real friction during the golden-reference-export plan (`docs/superpowers/plans/2026-08-10-brevitas-golden-reference-export.md`), where regenerating the model sometimes required 2+ retries to reach a converged state.

**Fixed**: `torch.manual_seed(DEFAULT_SEED)` now runs before `model = XOR()` in both `train()`'s default argument and `__main__`, so the seed actually controls initial weights. `DEFAULT_SEED` was chosen by sweeping seeds 0-79, training each to convergence, then running each through `quantized_model` and ranking by the post-quantization softmax margin (how far the correct-class probability sits above the decision boundary on all 4 truth-table rows) - seed `30` won, converging to `[0, 1, 1, 0]` both pre- and post-quantization with a margin of ~0.9995 (max possible is 1.0). Roughly half the swept seeds landed in the dead-ReLU degenerate optimum instead, confirming the bug was real and not a rare edge case. `train()`'s `seed` parameter now defaults to `DEFAULT_SEED` instead of `0`.

This closes the practical instability too: `deepsocflow/test/py/test_brevitas_sim.py`'s `test_json_has_single_quantization_point_per_bundle` still subprocess-runs `python -m deepsocflow.py.brevitas.xor` on every `pytest` invocation (retraining is still deliberate - see the 2026-08-10 Progress Log entry), and that retrain still overwrites `model/xor.pt`/`xor.onnx`/`model/xor_graph.json` in the working tree, so those three files will still show as modified in `git status` after running the test suite. But the retrain is now deterministic and always converges to `[0, 1, 1, 0]` rather than landing on a degenerate function depending on the draw - `git checkout -- deepsocflow/py/brevitas/model/` still restores the exact committed weights if you want a clean tree.

## Verilator version: `brew install verilator` (5.050) cannot run this design; the project's pinned 5.024 cannot be built on macOS

Two independent failures, discovered while getting the brevitas XOR model through an actual RTL simulation (2026-08-11):

- **Verilator 5.050 (current `brew install verilator`) segfaults.** `deepsocflow/firebridge/fb_top_verilator_wrap.cpp` drives the simulation with a loop that calls `top->eval()`; a DPI task (`run()`, invoked from *inside* that `eval()`) itself calls back into `at_posedge_clk()` → `step_time_veri()` → `top->eval()` again - i.e. `eval()` re-enters itself. Verilator 5's scheduler does not support that re-entrancy: the initial block that calls `run()` restarts from the top on every nested `eval()` call, printing `model_setup()`'s startup banner over a thousand times before the process dies with SIGSEGV (exit 139).
- **The project's own pin, `Dockerfile:22`'s `VERILATOR_VERSION=v5.024`, will not build on this machine's toolchain (Apple clang 21).** `verilated_timing.h`'s coroutine runtime includes `<experimental/coroutine>` unconditionally on libc++, which Apple clang 21 removed; patching that guard uncovers ~14 further errors inside 5.024's own `VlCoroutineHandle`/`std::multimap` machinery that don't compile against Apple clang 21's libc++ either. Verilator itself (the tool) builds fine in isolation - it's compiling *this design* against 5.024's headers that fails.
- **The README's "Verilator 5.014+" is misleading.** The real contract is whatever `Dockerfile:22` pins (`v5.024`), which is narrower than the README states and is a Linux/libstdc++-only build in practice on this hardware generation of macOS toolchain.
- **Resolution:** run Verilator 5.024 in a Linux container instead of on the host. `deepsocflow/sim/Dockerfile.sim` builds a minimal (verilator + build-essential only, no TensorFlow/PyTorch/RISC-V toolchain) Debian image pinned to `v5.024`; on Linux/libstdc++ its coroutine runtime compiles without incident. See the 2026-08-11 Progress Log entry for how to use it.

## `Hardware`'s default `axi_width=64` silently corrupts simulation output

Discovered via A/B comparison against the working `run/xor_qkeras.py` reference (2026-08-11): building `Hardware(...)` with default `axi_width` (`64`) makes the RTL simulation *run to completion and report no error*, but bundle 0's raw engine-layout output (`{ib}_y_raw...`) has values duplicated at some offsets and dropped at others - a data-alignment corruption, not a rounding difference. `run/xor_qkeras.py` passes cleanly because it explicitly sets `axi_width=128`; setting `axi_width=128` on the brevitas XOR model's `Hardware(...)` in `deepsocflow/py/brevitas/main.py` fixes it completely. The root cause is inside the legacy AXI DMA/burst-splitting RTL and was not chased down - out of scope for the brevitas port - but this is a **live trap for anyone starting a new model from `Hardware()`'s defaults**: the default configuration produces a simulation that exits 0 and looks like it passed while silently returning wrong data. Until this is root-caused, treat `axi_width=64` as untrusted and prefer `axi_width=128` (or whatever value a known-passing reference script uses) for any new model.

## `adapter.py`'s residual-add and mid-network-softmax attributes are wrong, but unreachable until conv/residual support lands

Found during the 2026-08-11 final-review fix wave (Part B of that review's findings) and deliberately **not fixed** - both are unreachable while this backend's adapter is dense-only with no residual-add/multi-output topology, so a real fix here would be untested speculative code for a capability that doesn't exist yet. Same family as the four real adapter defects the 2026-08-11 "brevitas → RTL end-to-end" Progress Log entry lists as fixed (`bias_val_shift`/`bias_b_shift`, `softmax_frac`/`softmax_max_i`, `is_flatten`/`is_softmax`) - whoever adds conv/pooling/residual support should read this first and check for more of the same shape before assuming the adapter is complete:

- `adapter.py:99` uses a `set` for `next_add_ibs`, but `xmodel.py:148` does `buf['out'][-1]`, which raises `TypeError` on a set (sets aren't indexable/ordered). Unreachable while residual-add is unsupported.
- `adapter.py:170-184` sets `self.softmax` from `cfg['softmax']` for any bundle, but only builds `pre_softmax` when that bundle is also the last one in the network. A mid-network softmax therefore reaches `xbundle.py:144`'s `self.pre_softmax.itensor` on a `None` `pre_softmax`, raising `AttributeError: 'NoneType' object has no attribute 'itensor'`.

# Progress Log

## 2026-08-10 - float XOR model, brevitas PTQ mapping, power-of-two quantization, QONNX + graph-JSON export

- **`deepsocflow/py/brevitas/xor.py`**: rewritten from a hand-quantized (`QuantLinear`/`XBundle`) model to a plain float `nn.Module` (`Linear → SiLU → Linear → SiLU → Linear → Softmax`, all `bias=True`). Added `train()` (Adam + `NLLLoss`, early-stop `patience`), the XOR truth-table data (`X`, `Y`), and `load()`/state_dict save to `model/xor.pt`. Fixed a bug where a single shared `self.silu` attribute (used twice in `forward()`) made `net.children()`'s order not match execution order, breaking `ptq.py`'s core/activation pairing — split into `silu_1`/`silu_2`.
- **`deepsocflow/py/brevitas/ptq.py`**: new `quantized_model` class that maps an arbitrary flat float `nn.Module` onto `xbundle`/`xlayer` (not `brevitas.graph.quantize`, since that doesn't cover `LeakyReLU`/`SiLU`/`SELU`/`GELU` - this project's own `xlayer/quantActivation.py` wrappers do). Built up incrementally:
  - `LAYER_MAP`/`ACT_MAP`/`POOL_MAP` (compute layers, activations, pooling → their `xlayer` equivalents), later refactored to live as local dicts inside their respective `_quantize_*` functions instead of module-level globals.
  - `quantization(calibration_data)`: runs `brevitas.graph.calibrate.calibration_mode` so quant scales are set from real activation stats instead of brevitas's fresh-init default.
  - `export(sample_input, path)`: QONNX export via `export_qonnx(..., dynamo=False)` - `dynamo=True` (PyTorch 2.12's new default) fails on brevitas's quant proxies (`DataDependentOutputException` on `aten.allclose`).
  - Switched every quantizer from float-scale (`Int8ActPerTensorFloat`, brevitas default weight quant) to power-of-two/"fixed-point" scale (`Int8WeightPerTensorFixedPoint`, `Int8ActPerTensorFixedPoint`, `Uint8ActPerTensorFixedPoint`) - required so hardware rescale is a pure bit-shift (`shift_round`), matching this project's RTL, not a real multiply/divide. `Int32Bias` needed no change: its scale = `input_scale * weight_scale`, automatically power-of-two once both inputs are.
  - Added `bias=True` support end to end (`XOR` now trains real biases; `_quantize_layer` quantizes them via `Int32Bias`).
  - Added per-layer configurable `weight_bits`/`bias_bits` (`quantized_model(net, weight_bits=8, bias_bits=32, layer_bits={"hidden_1": {"weight_bits": 4, ...}})`), keyed by the float net's own attribute names via `net.named_children()`.
  - Added `export_graph_json(sample_input, path)`: walks `self.bundles`, writes a JSON graph (per layer: type, shape, graph topology via `"input"`, and for weight/bias/input/act - `bits`, `frac`, `scale`, `zero_point`, `values` (int level), `values_float` (dequantized)).
- **`deepsocflow/py/brevitas/xor_qonnx.py`** (new file): builds the same XOR architecture directly from `xlayer`/`xbundle` (no `quantized_model` auto-mapping), loads `xor.py`'s trained float weights via a hand-written name map, calibrates, and exports its own graph JSON - kept in sync with `ptq.py`'s fixed-point quantizer choices.
- **`deepsocflow/py/brevitas/hardware.py`** (new file): ported the legacy `Hardware` class (`deepsocflow/py/hardware.py` - `export_json`/`export`/`export_vivado_tcl`/`simulate`, config→SystemVerilog/TCL generation) into the brevitas backend. Verified the class itself has zero qkeras/TensorFlow dependency, but importing anything under `deepsocflow.*` still pulls in TF/qkeras transitively via `deepsocflow/__init__.py`'s unconditional legacy imports - noted as a blocker for ever dropping TF as a dependency (see chat; not yet fixed, since it'd require touching files the qkeras backend still uses).
- Found and fixed a real bug live: setting `bias_bits=4` in `layer_bits` silently saturated every bias value (bias `frac` is *derived* as `input_frac + weight_frac`, not independently settable - 4 bits left no integer headroom) and flipped one XOR prediction wrong; fixed by raising to `bias_bits=16`.
- Confirmed (empirically, via `onnx.load` + `IntQuantTensor.value.dtype`) that both brevitas at runtime and the exported QONNX `Quant` nodes are "fake quantization" (quantize+dequantize round-trip, computed in float32) - not real int8 execution. Real integer/fixed-point execution requires either the RTL/hardware described by `hardware.py`'s config, or a from-scratch simulator (see Known Issues above for what's still missing there).
- Investigated the SiLU/non-piecewise-linear-activation gap in depth (see Known Issues entry above for the full write-up): confirmed the C firmware's `quant_lrelu` (`deepsocflow/c/runtime.h:153`) and the legacy `XActivation` (`deepsocflow/py/xlayers.py`) only ever supported `relu`/power-of-two-slope `leaky_relu` - so this is a real capability gap at the deployment layer, not just a JSON-export gap, and `Tanh` is in the exact same unsupported position as `SiLU` (`ptq.py`'s `ACT_MAP`/`quantActivation.py`'s `QuantTanh` wrapper exist but are fake-quantization-only, same as `QuantSiLU`). Read `hls4ml`'s activation-LUT source directly (legacy `nnet_activation.h` hardcoded-range LUTs for sigmoid/tanh/elu/selu; newer `hgq2/unary_lut.py` generic `UnaryLUT` requiring an input quantizer, indexed by raw two's-complement bits) as prior art. Empirically compared LUT vs PWL vs a "coefficient-LUT" (uniform bit-indexed PWL segments, no comparator chain, shift/add-only slopes) hybrid against the real `xor_graph.json` pipeline and against synthetic SiLU/tanh sweeps - the coefficient-LUT hybrid won on both accuracy and storage size; a "range-split" (fine LUT center + trivial constant tails) hybrid was also tried and measured to be worse, since neither function's curved region is narrow enough to isolate cheaply.
- **Scoping decision:** since the assigned task is porting qkeras → QONNX, not extending capability beyond what the C runtime (`quant_lrelu`) can execute, and the user wants to stay Python-only for now (not touch `deepsocflow/c/runtime.h`), the plan is to swap `xor.py`'s `SiLU` for `nn.LeakyReLU` (power-of-two slope) to get a fully deployable pipeline working end-to-end first, and defer the SiLU/Tanh LUT/PWL work (design already researched above) until after the pipeline itself is done.

## 2026-08-10 - brevitas golden-reference (`*_exp.txt`) export, bit-exactness fixes, single quantization point

- Found and fixed two real bugs in `sim.py`'s `FixedPointModel`, discovered by comparing its per-bundle int output against brevitas's own forward pass bundle-by-bundle (not just `argmax`, which was hiding the divergence): (1) `forward()` fed one bundle's output straight into the next without requantizing when their fracs differed (every `QuantLinear` had its own `input_quant`, creating a second quantization point after each activation that qkeras never has) - fixed by requantizing (`shift_round` + clip) whenever a producing bundle's `act_frac` doesn't match the consuming bundle's `input_frac`; (2) `quantize_input` didn't clip the rounded input to `input_bits`, so `X=1.0` at `frac=7` produced `128` (one past signed int8's `127`) instead of clipping like brevitas's own input quantizer does.
- `ptq.py`: switched to a **single quantization point per bundle** - only the first bundle's `QuantLinear` gets an `input_quant`; every later bundle consumes the previous bundle's activation output directly as an already-quantized `QuantTensor` (`return_quant_tensor=True` on every activation), letting `Int32Bias` resolve its scale from that tensor directly. Also narrowed unsigned (ReLU/Sigmoid) activations to `bits-1` (mirrors the legacy `xlayers.py:31-33`'s reasoning: an unsigned 8-bit value can reach 255, which wraps if ever packed into this project's otherwise-all-signed 8-bit words). Verified empirically against the real XOR model: predictions stay `[0,1,1,0]`, and Task 3's inter-bundle requant fix becomes a structural no-op (`input_frac` now always equals the previous bundle's `act_frac`) rather than a band-aid.
- `ptq.py::export_graph_json`: added `input_signed`/`weight.signed`/`bias.signed`/`act_signed` fields, closing the "no signed/unsigned flag per tensor" Known Issue.
- `sim.py`: added per-bundle `self.trace` (`x`/`y`/`acc`/`out` per bundle, `y` being the bias-free conv-sum), softmax handling (`self.pre_softmax`/`softmax_frac`/`softmax_max_i`/`softmax_out`, mirroring the legacy `xbundle.py`'s `2**17`-factor fixed-point softmax, but normalized per-row rather than by row 0's sum - the legacy formula is only correct for `batch_size==1`), a `RuntimeError` (not a cryptic `TypeError`) when `forward()` is called before `load_int_weights()`, and defaulting bias-less layers to zero (the JSON export only emits a `"bias"` key `if quant_bias is not None` - previously unhandled since XOR always has biases).
- New `deepsocflow/py/brevitas/export.py`: `export_inference(model, hw, x_float, batch_size=1)` writes `y_exp.txt` (`%f` if the last bundle has softmax, else `%d`) and `{ib}_y_nhwc_exp.txt` per bundle into `hw.DATA_DIR` - same filenames/formats/flatten order as the legacy `deepsocflow/py/xmodel.py::export_inference`, so a future RTL step can consume them unchanged. `check_hardware(model, hw)` asserts `input_bits`/`act_bits` fit within `hw.X_BITS` and that the ACC_WIDTH bound holds, plus (post-`forward()`) that traced activation outputs fall within their signed/unsigned range - it does NOT validate weight bits against `hw.K_BITS` or bias bits against `hw.B_BITS` (a real, separately-tracked gap; **closed 2026-08-11** - see that date's Progress Log entry). Engine-layout files (`{ib}_{ip}_{it}_*`, `.bin` blobs) are deliberately deferred - see the design spec's Phase 2 (importing the legacy `dataflow.py`'s reorder functions behind a brevitas-side adapter, rather than transcribing them, since they encode non-obvious hardware invariants that only an RTL diff - out of scope this session - would catch if ported wrong).
- `main.py`: now builds `FixedPointModel` from the graph JSON, loads int weights, runs the int forward pass, and calls `export_inference` with an XOR-sized `Hardware` (`bits_input=8, bits_weights=8, bits_bias=16, bits_sum=32` - `bits_sum` needs headroom for `ACC_WIDTH = K_BITS + X_BITS + clog2(in_features)`).
- New tests: `deepsocflow/test/py/test_brevitas_sim.py` (mostly unit tests needing no training - shift_round parity vs legacy, input clipping, inter-bundle requant, per-bundle trace, softmax, error handling, signed-flag precedence - except one integration test, `test_json_has_single_quantization_point_per_bundle`, which subprocess-runs `python -m deepsocflow.py.brevitas.xor` and so retrains the model on every run; an accepted, deliberate tradeoff, not fixed) and `deepsocflow/test/py/test_brevitas_export_inference.py` (end-to-end against the checked-in trained `xor.pt`, regenerating the graph JSON into `tmp_path` every run rather than reading the repo copy).
- Design doc: `docs/superpowers/specs/2026-08-10-brevitas-golden-reference-export-design.md`. Plan: `docs/superpowers/plans/2026-08-10-brevitas-golden-reference-export.md`.

## 2026-08-11 - brevitas → RTL end-to-end: adapter onto the legacy engine-layout exporter, RTL simulation passes on XOR

**Result: RTL simulation now passes for the brevitas XOR model, verified against the same Python fixed-point golden reference `sim.py`/`export.py` already produced.** `docs/superpowers/plans/2026-08-11-brevitas-rtl-end-to-end.md` / `.superpowers/sdd/2026-08-11-brevitas-rtl-end-to-end/progress.md` has the full nine-task ledger; this entry is the durable summary.

- **The adapter approach, and why.** The 2026-08-10 work (see above) got as far as a golden reference (`export.py::export_inference`) - flat `*_exp.txt` files nothing downstream reads yet. RTL actually consumes a different, denser file set: per-bundle engine-layout text/`.bin` blobs plus `config_fw.h`, produced by the legacy qkeras backend's `deepsocflow/py/xmodel.py` (`_export_bundles`) walking `XBundle`/`XDense`/`XActivation` objects (`deepsocflow/py/xbundle.py`, `deepsocflow/py/xlayers.py`). Two ways to get there were on the table: re-derive that file format independently in the brevitas backend, or reuse the legacy backend's own exporter by making brevitas's numbers look like an `XBundle` to it. **Reuse won** - the file format encodes non-obvious hardware invariants (bit-packing order, per-pass channel slicing, bias shift derivations) that only an RTL diff would catch if transcribed wrong, and the legacy code already carries that RTL-verified knowledge. The result is `deepsocflow/py/brevitas/adapter.py`: `BrevitasBundle`/`_Core`/`_Act` wear exactly the attribute surface `XBundle.export` (`xbundle.py`) and `config_fw.h`'s writer (`xmodel.py:234`) read, populated directly from `FixedPointModel.trace` (brevitas's own already-computed integer tensors) rather than recomputed - `BrevitasBundle.call_int` is a deliberate no-op, since re-deriving the bundle in integer arithmetic here would either duplicate `sim.py` or silently disagree with it. The module docstring states the split plainly: "brevitas owns the numbers; the legacy backend owns the file format." `export.py::export_rtl(model, hw, x_float, batch_size)` is the new entry point that drives it, calling `adapter.build_bundles()` then the legacy `xmodel.py::_export_bundles`.
- **`xmodel.py` preamble/bundle-loop split (commit `b52834c`, prerequisite refactor).** `export_inference` used to do keras-specific setup (clearing `hw.DATA_DIR`, printing "STARTING EXPORT") and the bundle-walking loop in one function. Split into a keras-only preamble (left in `export_inference`) and a keras-agnostic `_export_bundles(hw, x)` that only touches the `BUNDLES` global and `hw` - a pure move (+7 lines, no re-indentation, both were already module-level), verified byte-identical against the pre-split body. This is what lets the brevitas adapter call `_export_bundles` directly without dragging in any keras/TensorFlow import.
- **The qkeras XOR baseline (`run/xor_qkeras.py`) served two roles throughout:** (1) reference for what a passing RTL run looks like at all (used to A/B the axi_width=64 corruption below, and to confirm the Verilator-version diagnosis before touching this branch's own code), and (2) a standing regression - re-run after nearly every task to confirm nothing in the shared legacy path (`xbundle.py`, `xmodel.py`, `dataflow.py`) broke. It caught a real regression: commit `d3091e2` ("brevitas layers added") had deleted `ic_right += CM_p` from `XBundle.export`'s per-pass loop while adding comments, leaving `ic_left`/`ic_right` both stuck at 0 so every pass sliced input channels `[0:0]` (`filter depth must be strictly positive, got 0`). This is a pre-existing-code regression, not something introduced by the brevitas port, and it broke the qkeras path too (`run/example.py`). Fixed by restoring the increment as an extracted `_pass_channel_slices(r)` helper plus 4 unit tests (commit `65e50b2`); the qkeras baseline (`run/example.py`, all 7 bundles) and `run/xor_qkeras.py` both pass RTL sim again.
- **The four-row export.** `export_rtl`'s `batch_size` defaults to `4` (all four XOR truth-table rows), not `1` like `export_inference`'s golden-reference `batch_size` convention - the RTL testbench is driven once per invocation and needs the whole truth table in a single pass to be a meaningful hardware-level check, whereas the Python-only golden reference only ever needed to prove per-row bit-exactness.
- **Adapter defects, all one family.** Legacy `XBundle` populates several attributes (`bias_val_shift`/`bias_b_shift`, `softmax_frac`/`softmax_max_i`, `is_flatten`/`is_softmax`) inside `call_int`, which the adapter deliberately never runs - so each had to be supplied explicitly, and each was missed at least once before `_export_bundles` was actually exercised end to end: (1) `bias_val_shift`/`bias_b_shift` missing entirely (`config_fw.h`'s writer reads them at `xmodel.py:234`) - fixed by setting both to `0`, provably so rather than by assumption: `add_val_shift` (`utils.py:66-81`) returns `(max(y.frac,b.frac)-y.frac, max(y.frac,b.frac)-b.frac)`, and brevitas's single-quantization-point-per-bundle design makes `bias_frac == acc_frac` always (`sim.py:166` asserts it outright), so both differences are identically zero. (2) `softmax_frac`/`softmax_max_i` missing, defaulted per `xbundle.py:47-48`, real values read off `sim.py:190,194`. (3) `is_flatten`/`is_softmax` stored as `False` instead of `None` when absent - `xmodel.py:233` emits them with `is not None`, while legacy itself stores `None` (`xbundle.py:41,44`) and tests truthiness elsewhere (`xbundle.py:144`, `xmodel.py:221`); `False` satisfies neither convention and made every bundle claim to be both flattened and softmaxed. **The lesson that stopped the bleeding:** none of these were catchable by testing the adapter's own attributes or even `.export()` in isolation - they only became visible once a test drove the *real* `_export_bundles` path and asserted on the emitted `config_fw.h` text, the level each bug was actually visible at (`test_full_legacy_export_path_runs_on_adapted_bundles`, `test_legacy_xbundle_export_runs_on_adapted_bundles`). Anyone extending the adapter for a new layer type should assume the same failure mode: any attribute normally set inside `call_int` needs to be supplied explicitly and verified against a real `config_fw.h` dump, not inferred from the dataclass shape.
- **Two environment traps, now load-bearing enough to also live as their own Known Issues above:** the Verilator version pin (`brew`'s 5.050 segfaults on this design's re-entrant `eval()`; the project's pinned `v5.024` cannot be built against Apple clang 21's libc++; resolved by running `v5.024` inside a Debian container) and `Hardware`'s default `axi_width=64` silently corrupting bundle-0 output (fixed by `axi_width=128`, matching `run/xor_qkeras.py`).
- **`site-packages/deepsocflow.pth` points at a different checkout** (the non-worktree main repo clone). Any script run as `cd run && python ...` silently executes that other copy instead of this worktree's code - Task 1.5 initially "passed" against the wrong code this way. Always run from the worktree root (cwd wins over the `.pth` entry on `sys.path`), or use `deepsocflow/sim/docker_sim.py`, which inserts the worktree root at `sys.path[0]` itself.
- **New tooling moved into the repo: `deepsocflow/sim/`** (`Dockerfile.sim`, `run-sim-in-docker.sh`, `docker_sim.py`). These were working-session scratch files outside the repo during this task; without moving them, the only way to run a simulation on this machine would need to be rediscovered from scratch next time. `docker_sim.py` monkeypatches `deepsocflow.py.hardware.Hardware.simulate` (the **legacy** `Hardware` class - the brevitas backend's own `deepsocflow/py/brevitas/hardware.py::Hardware` is a separate, non-inheriting class kept deliberately free of the TensorFlow/qkeras import chain) to shell out to `run-sim-in-docker.sh` instead of invoking a local `verilator`/`xsim` binary. (**Updated 2026-08-11**: `docker_sim.py` now patches both `Hardware` classes directly and unconditionally - `main.py` originally had a `sys.modules`-based guard meant to bridge the two only "when actually running under `docker_sim.py`", but that guard was always true in practice (importing `deepsocflow.py.brevitas.export` alone already pulls in the legacy module transitively) and its comment was false; see that date's Progress Log entry.) Both `WORKTREE` (in `docker_sim.py`) and the mount path (in `run-sim-in-docker.sh`) are derived from each script's own file location (matching `hardware.py`'s `_PACKAGE_ROOT` pattern) rather than hardcoded, so this works from any clone/worktree.
- **Exact commands to reproduce the RTL run** (from the worktree root; needs Docker - `colima start` first if `docker info` fails):
  ```
  python -m pytest deepsocflow/test/py/ -q
  python deepsocflow/sim/docker_sim.py deepsocflow/py/brevitas/main.py
  ```
  The pytest run takes seconds (42 tests as of this entry; 45 after the 2026-08-11 final-review fix wave below added three). The docker run takes several minutes (Verilator build + sim) and should end with `Bundle 0/1/2 ... Passed` and `brevitas XOR: RTL simulation PASSED`. The same tooling also still runs the pre-existing qkeras scripts unchanged: `python deepsocflow/sim/docker_sim.py run/xor_qkeras.py` and `python deepsocflow/sim/docker_sim.py run/example.py`.
- **Scope of what "passes" actually covers:** the brevitas adapter (`adapter.py`) supports dense (`XDense`) bundles only - no conv/pooling/residual. Those paths are exercised in this repo only by the pre-existing qkeras `run/example.py`, not by anything brevitas-driven. Don't read "RTL simulation passes" as validating conv/pooling/residual through the brevitas backend; it validates the dense/activation/softmax path only, on an XOR-sized model.
- **One honest caveat about what the three per-bundle `Passed` lines actually prove:** bundle 2's reported `Error` is not exactly `0` (it's a small float like `7.152557373046875e-06`) because `xmodel.py:384` checks it with `atol=0.5` against probabilities in `[0,1]` - an assertion that cannot fail for any real softmax output. The real evidence that RTL matches the Python model bit-exactly is bundles 0 and 1, whose `Error` is exactly `0`. Separately, `sim.py:194` normalizes softmax per row while the hardware uses one global `softmax_max_i` (`adapter.py:182` collapses to it correctly for export) - nothing in the test suite verifies the two normalization schemes agree in general, only that they happen to produce a passing comparison on this specific batch-of-4 XOR input.
- Commits this session: `65e50b2` (Task 1.5, `_pass_channel_slices` regression fix), `33aa6dc` (Task 2, qkeras baseline confirmed), `223e557`, `9b2eece` (Tasks 3-5, preamble split + `act_params`/layout helpers), `5eb94b3`/`24f5ed2`/`542a503` (Task 6/6.5/6.6, adapter), `1402b10`/`cf2ce64` (Task 7/7.5, `export_rtl` + `is_flatten`/`is_softmax` fix), `ee857d9` (Task 8, RTL simulation passing).

## 2026-08-11 - final whole-branch review: fix wave (A1-A7) + Part B known issues recorded

Verdict on the 2026-08-11 "brevitas → RTL end-to-end" branch was "fit to merge after two small fixes"; this entry covers all seven Part-A findings from that review (`.superpowers/sdd/2026-08-11-brevitas-rtl-end-to-end/final-review-findings.md`), fixed in one wave, plus Part B recorded as Known Issues above (`adapter.py`'s residual-add/mid-network-softmax defects) rather than fixed, since both are unreachable while conv/residual support doesn't exist.

- **A1 - the `sys.modules` guard in `main.py` was always true.** `import deepsocflow.py.brevitas.export` alone already pulls the legacy `deepsocflow.py.hardware` module (and TensorFlow) into `sys.modules` transitively via `deepsocflow/__init__.py`, so the guard's `if _legacy_hardware_module is not None` fired on every run regardless of whether `docker_sim.py` was involved, and its comment claiming "TF/qkeras stay unimported" otherwise was false. Fixed by moving the bridge to where it belongs: `docker_sim.py::main()` now imports and patches **both** `Hardware` classes explicitly and unconditionally (`deepsocflow.py.hardware.Hardware.simulate` and `deepsocflow.py.brevitas.hardware.Hardware.simulate`, both set to `_docker_simulate`) right after it patches the legacy one. `main.py` no longer imports or references the legacy module at all. Verified this restructuring is not load-bearing in some way the findings file didn't anticipate: the full end-to-end run (both a warm-image run and the cold-rebuild run below) still prints `Bundle 0, Error: 0. Passed` / `Bundle 1, Error: 0. Passed` / `Bundle 2 ... Passed` / `brevitas XOR: RTL simulation PASSED` after this change.
- **A2 - no `K_BITS`/`B_BITS` validation on the brevitas path (the more consequential of the two "small fixes").** `adapter.py`'s `build_bundles` labels every weight tensor `bits=hw.K_BITS` and every bias tensor `bits=hw.B_BITS` regardless of the JSON's real per-bundle bit-width - so a mismatch (e.g. a per-layer `bias_bits` wider than `Hardware(bits_bias=...)`) doesn't raise; it silently produces wrong `.bin` blobs (xmodel.py's `astype(np.int16)` truncation-wraps oversized bias values). `check_hardware` (`deepsocflow/py/brevitas/export.py`) now asserts `weight.bits == hw.K_BITS` (mirrors legacy `xmodel.py:56`'s equality check - weight width is one hardware-wide packing width, not per-layer) and `bias.bits <= hw.B_BITS` (mirrors `xmodel.py:57`'s `>=` - bias only needs headroom), both with an assertion message naming the bundle and both values. This required a **prerequisite, purely-additive change to `sim.py`** (outside the review's originally-named file list, called out explicitly here so a re-reviewer isn't surprised by it): `FixedPointModel`'s `_build_topology` did not surface `weight.bits`/`bias.bits` at all - only `weight_frac`/`bias_frac`, since the integer forward pass never needed the bit-width, only the fractional point. Added `weight_bits`/`bias_bits` keys to each bundle's dict (`bias_bits=None` when the bundle has no bias, matching the existing `bias_frac` fallback pattern) - no behavior change to `forward()`/`quantize_input()`/anything else in `sim.py`, purely new data `check_hardware` needed to validate against. Two new tests in `test_brevitas_export_inference.py` reproduce the exact failure scenarios from the review: `test_hardware_weight_bits_mismatch_raises` (`layer_bits={'hidden_1': {'weight_bits': 4, ...}}` against `Hardware(bits_weights=8)`) and `test_hardware_bias_bits_exceeding_b_bits_raises` (`bias_bits=32` against `Hardware(bits_bias=16)`, the exact scenario the review's finding described).
- **A3 - `act_params('leaky_relu')` with the default slope raised the wrong error.** `negative_slope` defaults to `0.0`; `math.log2(0.0)` raises `ValueError: math domain error` before the power-of-two assertion (the pipeline's only power-of-two enforcement) could produce its legible message. Fixed with a `negative_slope > 0` guard in `adapter.py::act_params` before the `log2` call, raising the same assertion message legacy's `np.log2`-degrades-to-`-inf` behavior produces. New test: `test_act_params_rejects_default_slope_with_legible_message` in `test_brevitas_adapter.py`.
- **A4 - generated files landing untracked in the source tree.** Consolidated `.gitignore` additions for both locations the documented reproduce command writes into: extended the existing `run/` block with `config_hw.tcl`/`config_tb.svh`/`util.txt`/`mem_bytes.txt`/`vivado_flow.tcl` (previously only `config_fw.h`/`config_hw.svh`/`hardware.json`/`sources.txt` were covered there), and added a new `deepsocflow/py/brevitas/` block for the same nine filenames (that directory had no dedicated ignores before beyond the generic `**/vectors/*`/`build/` patterns). Checked `git ls-files` first against every pattern added - none of the newly-ignored filenames were tracked.
- **A5 - `run-sim-in-docker.sh` never built the image it needs.** Added a `docker image inspect "$IMAGE" || docker build ...` guard at the top of the script (building from `deepsocflow/sim/Dockerfile.sim`), corrected the header comment (was "Builds and runs" referring only to the verilator build, not the image), and added a comment pointing at `hardware.py`'s verilator invocation (`deepsocflow/py/hardware.py:232-257` and the brevitas-backend counterpart) since the script's argv is a hand-kept duplicate of it, not generated - explicitly not refactored into one shared place per the review's scope note. **Verified the auto-build invocation fires**: `docker image rm deepsocflow-sim:v5.024`, then the same end-to-end reproduce command - the runner detected the missing image and ran `docker build`, and the simulation passed afterward. Worth being precise about what this does and doesn't prove: `docker image rm` only drops the local tag, not the builder's layer cache, so that rebuild (~90s, then a warm-cache Verilator compile/sim) reused cached layers rather than compiling verilator from source again - it verifies the `docker image inspect` guard correctly detects "no image" and invokes `docker build`, which is what A5 was actually about, but it is not proof the `Dockerfile.sim` recipe itself still builds correctly with an empty cache (e.g. after `docker builder prune`). That stronger check was not run here (see the fix report for the exact commands/output).
- **A6 - blank line inside the `apt-get install ... \` continuation in `Dockerfile.sim`.** Removed the blank line between `python3 \` and `ccache \` (BuildKit's `EmptyContinuation` warning / hadolint failure).
- **A7 - `main.py` double export.** `main.py` called both `export_inference` (batch_size=1, writes `y_exp.txt`/`{ib}_y_nhwc_exp.txt` into `hw.DATA_DIR`) and then `export_rtl` (batch_size=4), whose underlying `_export_bundles` (`xmodel.py:319-334`) deletes that same directory and rewrites those same two file kinds itself as part of the engine-layout export - so the first call was dead work whose success print named paths that were about to be overwritten. Removed the `export_inference` call and its import from `main.py`; `export_rtl` is now the only export call. `export_inference` itself is untouched and still exported/tested - callers that only want the golden-reference files without the engine-layout ones still have it.
- **Part B (not fixed, recorded as Known Issues above):** `adapter.py`'s `next_add_ibs` `set` (vs. `xmodel.py:148`'s indexing) and its `pre_softmax`-only-built-when-last bug are both real defects but unreachable while residual-add/mid-network-softmax don't exist in this dense-only adapter - fixing them now would be untested speculative code. Recorded verbatim in the "Known Issues" section above for whoever adds conv/pooling/residual support next.
- **Verification:** `python -m pytest deepsocflow/test/py/ -q` → 45 passed (42 baseline + 3 new: A3's adapter test, A2's two export_inference tests). `python deepsocflow/sim/docker_sim.py deepsocflow/py/brevitas/main.py` run twice - once against the pre-existing image, once after `docker image rm deepsocflow-sim:v5.024` to exercise A5's auto-build path (see that bullet for the honest caveat on what this does and doesn't prove) - both ending in `Bundle 0, Error: 0. Passed` / `Bundle 1, Error: 0. Passed` / `Bundle 2, Error: 7.152557373046875e-06. Passed` / `brevitas XOR: RTL simulation PASSED`.
- Full findings-to-fix mapping and raw command output: `.superpowers/sdd/2026-08-11-brevitas-rtl-end-to-end/final-fix-report.md`.

## 2026-08-11 - reproducible XOR seed lock, LeakyReLU power-of-two guard in `ptq.py`, macOS simulation docs

Closed the two Known Issues left over from the RTL end-to-end work that were explicitly deprioritized as "not required for XOR to pass RTL sim" (see the two `Closed 2026-08-11` entries above for the full write-up) - both are now fixed, not just documented:

- **`xor.py`**: `torch.manual_seed(DEFAULT_SEED)` now runs before `model = XOR()` (both in `train()`'s default and in `__main__`), actually fixing the bug instead of just describing it. `DEFAULT_SEED = 30`, chosen by sweeping seeds 0-79 (script not checked in - one-off, results reproducible by rerunning the sweep): trained each to convergence, quantized each through `quantized_model`, and ranked by post-quantization softmax margin. Seed 30 converges to `[0, 1, 1, 0]` pre- and post-quantization with a ~0.9995 margin (max 1.0); roughly half the swept seeds instead landed in the dead-ReLU degenerate optimum, confirming this was a real, frequent failure mode and not a tail case. Verified deterministic by running `python -m deepsocflow.py.brevitas.xor` twice and diffing the printed predictions/qm output byte-for-byte - identical both times.
- **`ptq.py`**: `_quantize_activation` now asserts `nn.LeakyReLU.negative_slope` is a negative power of two before wrapping it in `QuantLeakyReLU`, mirroring the check `adapter.py::act_params` already enforced much later in the pipeline (at RTL-adapter time) and `xlayers.py:23`'s legacy equivalent. Verified against the default slope (`0.01`) and a non-power-of-two slope (`0.1`) both raising with a legible message, and a valid slope (`0.125`) passing.
- **New `deepsocflow/sim/README.md`**: consolidates the environment know-how that was previously only in this file's Known Issues/Progress Log (the Verilator 5.050-segfaults-vs-5.024-can't-build-on-Apple-clang split, why the fix is a Linux container, `docker_sim.py`/`run-sim-in-docker.sh` usage, the `axi_width=128` trap, the `.pth`-points-at-a-different-checkout trap, and the mount-path-must-match-`sources.txt` reasoning) into one place aimed at a new contributor setting up macOS simulation from scratch, rather than someone already mid-session with this file loaded.
- Verification: `python -m pytest deepsocflow/test/py/ -q` → 45 passed, unchanged from before this session's changes (confirms the seed lock and the new assert didn't regress anything already covered).
- Non-piecewise-linear activation support (SiLU/Tanh/etc.) was explicitly left out of scope for this session - see the still-open Known Issue above for the researched design.

## 2026-08-11 - FPGA/Vivado path scoped out: `export_vivado.py`, PYNQ driver notebook is external, `xor_qonnx.py` removed as dead code

The brevitas pipeline had never been driven past RTL simulation to an actual bitstream/FPGA run - this session scoped out what that would take and added the one missing piece that's actually this repo's responsibility.

- **The user has a real target: `geonosis`, a Linux server with Vivado v2023.2 already installed** (confirmed by running `vivado -mode batch -source vivado_flow.tcl` there - it printed the full Vivado banner and failed only on the missing `.tcl` file, not on `vivado` itself). Board choice: **`zcu104`** (Xilinx UltraScale+, licensed already on that server) - not `pynq_z2`, despite an unrelated PYNQ driver notebook also being in play (see below). `deepsocflow/tcl/fpga/zcu104.tcl` was checked and does NOT have the hardcoded-Windows-path bug that `pynq_z2.tcl` has (`board.repoPaths` pointing at a `C:/Users/...` path) - zcu104 is a standard Xilinx board with board files normally bundled in Vivado, so no extra fix was needed for this board choice. (If `pynq_z2` is ever targeted instead, that hardcoded path in `pynq_z2.tcl` will need fixing first.)
- **New `deepsocflow/py/brevitas/export_vivado.py`**: standalone script that builds the same `Hardware(...)` config `main.py` uses (`processing_elements=(8,24)`, `bits_input=8, bits_weights=8, bits_bias=16, bits_sum=32`, `axi_width=128`) and calls `hw.export()` + `hw.export_vivado_tcl(board=...)` to produce `config_hw.tcl`/`.svh`, `config_tb.svh`, `sources.txt`, `vivado_flow.tcl`. Deliberately loads `hardware.py` via `importlib` from its file path instead of `import deepsocflow...`, so it needs only `numpy` - not the full torch/brevitas/TensorFlow stack `deepsocflow/__init__.py` otherwise pulls in (verified empirically: `'tensorflow' in sys.modules` / `'torch' in sys.modules` both `False` after loading it this way). This matters because `Hardware.MODULE_DIR` (and therefore every path these exports write) is computed from `os.path.abspath(__file__)` at runtime, so the script **must run on the machine that will actually run Vivado** (its own docstring says this explicitly) - paths generated on one machine and copied to another are wrong. Confirmed the repo being public on GitHub (`Charaphat/cgra4ml`) makes `git clone` on `geonosis` simpler than manually copying files - since this branch (`cgra4ml-macos-simulation`) is already pushed there, `git clone` + `git checkout cgra4ml-macos-simulation` + `cd deepsocflow/py/brevitas && python export_vivado.py --board zcu104` is the full path to a generated `vivado_flow.tcl` ready for `vivado -mode batch -source vivado_flow.tcl`.
- **A separate PYNQ-based Jupyter notebook the user has (`CGRA4ML_PYNQ.ipynb`, uploaded from outside this repo) is NOT part of this codebase.** It drives a real FPGA over PYNQ (`pynq.Overlay` loading a `design_1.bit` bitstream, then a `DeepSoCFlowPYNQ` driver class reading `config.json`/`wbx.bin`/comparing against `y_exp.txt`) - `wbx.bin`/`y_exp.txt` match this project's own export filenames exactly (confirmed these are literally produced by `export_rtl`'s underlying `_export_bundles`), and `config.json`'s expected content lines up with this project's `hardware.json` (`hw.export_json()`'s output) - the physical address the notebook prints (`0xb0000000`) matches `hardware.json`'s `config_baseaddr: "B0000000"` exactly. But the actual driver module the notebook imports, `pynq_driver.py` (class `DeepSoCFlowPYNQ`), **does not exist anywhere in this repo or its git history** (confirmed by grep across the working tree and `git log --all -- "*pynq*"`) - it must come from wherever the user got the notebook. `deepsocflow/tcl/fpga/pynq_z2.tcl` exists (a board script, referenced by `run/matmul.py`'s `board='pynq_z2'` call) but that's the RTL/synthesis layer, not the PYNQ Python driver layer the notebook needs - two different things that happen to share a board name. Nothing was written to reconcile these; recorded here so a future session doesn't waste time searching this repo for `pynq_driver.py` again.
- Also found and referenced (not this repo's code, but a useful prior-art example while scoping the above): another qkeras-based training script the user has, external to this repo, calling `hw.export_vivado_tcl(board='zcu102')` inline right after training/`hw.export()`, and running `verify_inference(..., SIM='xsim', SIM_PATH='/home/software/Xilinx/Vivado/2023.2/bin/')` - i.e. using Vivado's own `xsim` simulator directly rather than Verilator. This confirms `geonosis`-class lab servers with Vivado installed can skip this repo's whole Docker/Verilator workaround (`deepsocflow/sim/`) for RTL simulation too, not just for the bitstream step - both this backend's `hardware.py` and the legacy one already have an `if SIM == 'xsim':` branch in `simulate()`. Not wired up or tested against the brevitas backend this session - noted as a real option for whoever next needs to simulate on a Vivado-equipped machine instead of via Docker.
- **Removed `deepsocflow/py/brevitas/xor_qonnx.py` as dead code.** It was a hand-built alternate path to `ptq.py`'s `quantized_model` auto-mapping (see the 2026-08-10 Progress Log entry for why it was originally added - kept "in sync with `ptq.py`'s fixed-point quantizer choices" as a cross-check). Confirmed via repo-wide grep that nothing imports or otherwise references it - not `main.py`, not any test. If a from-scratch cross-check of `ptq.py`'s auto-mapping is needed again later, this file's git history (removed in commit `e780f5b`) has the reference implementation.
- **Housekeeping**: deleted all locally-generated, gitignored export artifacts across `run/` and `deepsocflow/py/brevitas/` (`config_fw.h`, `config_hw.svh/.tcl`, `hardware.json`, `sources.txt`, `mem_bytes.txt`, `util.txt`, `build/`, `vectors/`, `run/reference_qkeras_xor/`, `run/vectors_qkeras_xor/`) and the three checked-in model files (`model/xor.pt`/`xor.onnx`/`xor_graph.json`) at the user's request. Worth knowing for next time: **running the test suite regenerates the three model files as a side effect** (the same `test_json_has_single_quantization_point_per_bundle` subprocess-retrain mentioned throughout this file) - after this session's `pytest` run, they reappeared **byte-identical** to the committed versions, a nice incidental confirmation that the `DEFAULT_SEED=30` reproducibility fix above actually holds under a real delete-and-regenerate cycle, not just a rerun-in-place one.
